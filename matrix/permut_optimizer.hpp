#pragma once

// p = blocksPerInstance
/** 
 *  @param blockPerInstance The amount of blocks per instance.
 *  @param blockDimension The dimension of the partitioned square matrix.
 **/
void find_optimal_permutation(int blocksPerInstance, int blockDimension);
